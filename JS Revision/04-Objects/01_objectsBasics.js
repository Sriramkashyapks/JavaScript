// objects is a collection of key value pairs which helps in storing the data in a structured way

const fruits = {
    apple : 10,
    banana : 20,
    orange : 30,
}

console.log(fruits.apple);
console.log(fruits.banana);