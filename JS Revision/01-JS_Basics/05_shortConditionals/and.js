const age = 25;
const canDrive = true;

if (age >= 18 && canDrive) { // here the age is greater than 18 and canDrive is true then the if statement is executed
    console.log("You are eligible to drive.");
} else {
    console.log("You are not eligible to drive.");
}

// This operator compares the 2 conditions is both the conditions are true then only the if statemnt is executed