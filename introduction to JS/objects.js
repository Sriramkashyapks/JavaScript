let obj = { name: "John", age: 30 };
obj.age = 31; // the age property of obj is now 31
console.log(obj);

// The above code creates an object obj with properties name and age. 
//The age property is then modified to 31. The output of the above code will be: 
// { name: 'John', age: 31 }

// objects conatin key value pairs where key is a string and value can be any data type